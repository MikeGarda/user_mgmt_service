name: Build & Promote (GitOps)

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  BACKEND_IMAGE:  ghcr.io/mikegarda/user_mgmt_service-backend
  FRONTEND_IMAGE: ghcr.io/mikegarda/user_mgmt_service-frontend
  OPS_REPO:       MikeGarda/vsc-ops
  VALUES_PATH:    k8s_helm/values.yaml

jobs:
  build-and-promote:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout app repo
        uses: actions/checkout@v4

      - name: Kurzer Commit-Hash als Image-Tag
        id: vars
        run: echo "tag=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"

      - name: Login GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build & Push Backend
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.BACKEND_IMAGE }}:${{ steps.vars.outputs.tag }}
            ${{ env.BACKEND_IMAGE }}:latest

      - name: Build & Push Frontend
        uses: docker/build-push-action@v6
        with:
          context: ./src-ui
          file: ./src-ui/Dockerfile
          push: true
          build-args: |
            NEXT_PUBLIC_API_URL=http://backend:8080
          tags: |
            ${{ env.FRONTEND_IMAGE }}:${{ steps.vars.outputs.tag }}
            ${{ env.FRONTEND_IMAGE }}:latest

      - name: Promotion - neuen Tag ins Ops-Repo committen
        env:
          GH_TOKEN: ${{ secrets.OPS_REPO_TOKEN }}
          TAG: ${{ steps.vars.outputs.tag }}
        run: |
          set -e
          sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq
          git clone "https://x-access-token:${GH_TOKEN}@github.com/${OPS_REPO}.git" ops
          cd ops
          yq -i ".images.backend.tag  = \"${TAG}\"" "${VALUES_PATH}"
          yq -i ".images.frontend.tag = \"${TAG}\"" "${VALUES_PATH}"
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add "${VALUES_PATH}"
          git commit -m "promote: images -> ${TAG}" || echo "keine Aenderung"
          git push
